  <div class="methods">    <h4>支付方式</h4>    <div class="balance">          <p class="show">使用余额&nbsp;&nbsp;        <input type="text" id="useOver" data-amount="{$aCart.final_amount}" data-money="{$uinfo.money}"  {if $uinfo.money lte 0} readonly="readonly" {/if} />        &nbsp;&nbsp;&nbsp;&nbsp;当前可用余额&nbsp;<span id="remaining">{$uinfo.money|price}</span></p>      <p class="enough">您还需要支付&nbsp;<span id="also_amount" >{$aCart.amount|price}</span>&nbsp;，请选择以下麦富迪合作机构来完成支付。</p>    </div>    <div class="total" id="total">     {foreach from=$payments item=list name=pay}      <p class="jxgouwu {if $smarty.foreach.pay.first}current{/if}" data-code={$list.payment_code} data-payid={$list.payment_id}><a href="#" target="_blank">{$list.payment_name}</a></p>      {/foreach}    </div>  </div>